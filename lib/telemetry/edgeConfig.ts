// This file configures the initialization of Sentry on the edge.
// Minimal stub to prevent import errors during development

export const sentryEdgeConfig = {
  initialized: false,
};

// No-op initialization for development
export default sentryEdgeConfig;
